
services:
  caddy:
    container_name: caddy
    image: caddy
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./caddy/caddy-config:/config
      - ./caddy/caddy-data:/data
      - ./caddy/caddyfile:/etc/caddy/Caddyfile
      - ./caddy/html/index.html:/usr/share/caddy/index.html

volumes:
  caddy-config:
  caddy-data:

